name: Build on Jenkins
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger Jenkins Job
      run: curl -u admin:admin -X POST "http://192.168.115.133:8080/job/pruebaGit/build?token=token_Testeo"
